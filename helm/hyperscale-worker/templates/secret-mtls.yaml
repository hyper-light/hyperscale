{{- if eq .Values.mtls.source "secret" }}
{{- if empty .Values.mtls.secret.name -}}
{{- fail "mtls.source=\"secret\" requires mtls.secret.name to be set" -}}
{{- end }}
# mTLS Secret {{ .Values.mtls.secret.name }} (namespace {{ .Release.Namespace }})
# is mounted by the Deployment from an externally-managed Secret.
{{- end }}
