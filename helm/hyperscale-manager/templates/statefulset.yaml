apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "hyperscale-manager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hyperscale-manager.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "hyperscale-manager.headlessServiceName" . }}
  replicas: {{ .Values.replicas }}
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      {{- include "hyperscale-manager.selectorLabels" . | nindent 6 }}
  updateStrategy:
    type: {{ .Values.updateStrategy.type }}
    {{- if eq .Values.updateStrategy.type "RollingUpdate" }}
    rollingUpdate:
      maxUnavailable: {{ .Values.updateStrategy.rollingUpdate.maxUnavailable }}
      partition: {{ .Values.updateStrategy.rollingUpdate.partition }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "hyperscale-manager.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        checksum/seeds-configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "hyperscale-manager.serviceAccountName" . }}
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.securityContext.pod | nindent 8 }}

      affinity:
        podAntiAffinity:
          {{- if .Values.podAntiAffinity.enforceNodeSpread }}
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  {{- include "hyperscale-manager.selectorLabels" . | nindent 18 }}
              topologyKey: kubernetes.io/hostname
          {{- end }}
        {{- with .Values.affinity }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

      topologySpreadConstraints:
        - maxSkew: {{ .Values.topologySpread.zoneSkew }}
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              {{- include "hyperscale-manager.selectorLabels" . | nindent 14 }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      containers:
        - name: manager
          image: {{ include "hyperscale-manager.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext.container | nindent 12 }}

          ports:
            - name: cluster
              containerPort: {{ .Values.listen.port }}
              protocol: TCP
            - name: swim
              containerPort: {{ .Values.listen.swimPort }}
              protocol: UDP
            - name: metrics
              containerPort: {{ .Values.listen.metricsPort }}
              protocol: TCP

          args:
            - "--cluster-id"
            - {{ .Values.cluster.id | quote }}
            - "--mtls-cert"
            - "{{ .Values.mtls.mountPath }}/{{ .Values.mtls.secret.certKey }}"
            - "--mtls-key"
            - "{{ .Values.mtls.mountPath }}/{{ .Values.mtls.secret.keyKey }}"
            - "--mtls-ca"
            - "{{ .Values.mtls.mountPath }}/{{ .Values.mtls.secret.caKey }}"
            - "--listen-address"
            - "{{ .Values.listen.host }}:{{ .Values.listen.port }}"
            - "--swim-port"
            - {{ .Values.listen.swimPort | quote }}
            - "--datacenter-id"
            - {{ .Values.cluster.datacenterId | quote }}
            - "--advertised-address"
            - "$(POD_NAME).{{ include "hyperscale-manager.headlessServiceFQDN" . }}:{{ .Values.listen.port }}"
            {{- if .Values.listen.verifyAdvertisedAddress }}
            - "--verify-advertised-address"
            - "true"
            {{- end }}
            {{- if .Values.bootstrap.enabled }}
            - "--initial-members"
            - {{ include "hyperscale-manager.initialMembers" . | quote }}
            - "--cluster-size"
            - {{ .Values.replicas | quote }}
            {{- end }}
            - "--seeds"
            - {{ include "hyperscale-manager.seeds" . | quote }}
            {{- if gt (len .Values.cluster.gateSeeds) 0 }}
            # AD-52 §17 federation: register this DC with the gate cluster.
            - "--gate-seeds"
            - {{ include "hyperscale-manager.gateSeeds" . | quote }}
            {{- end }}
            # AD-52 §21 tuning surface.
            - "--bootstrap-window"
            - {{ .Values.tuning.bootstrapWindow | quote }}
            - "--seed-refresh-interval"
            - {{ .Values.tuning.seedRefreshInterval | quote }}
            - "--learner-promote-threshold"
            - {{ .Values.tuning.learnerPromoteThreshold | quote }}
            - "--learner-max-lifetime"
            - {{ .Values.tuning.learnerMaxLifetime | quote }}
            - "--tombstone-retention"
            - {{ .Values.tuning.tombstoneRetention | quote }}
            - "--disconnected-mode-threshold"
            - {{ .Values.tuning.disconnectedModeThreshold | quote }}
            - "--snapshot-interval-entries"
            - {{ .Values.tuning.snapshotIntervalEntries | quote }}
            - "--max-inflight-appendentries"
            - {{ .Values.tuning.maxInflightAppendEntries | quote }}
            - "--max-seed-candidates"
            - {{ .Values.tuning.maxSeedCandidates | quote }}
            {{- if .Values.tuning.leaderLeaseEnabled }}
            - "--leader-lease-enabled"
            - "true"
            {{- end }}

          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}

          {{- if eq .Values.probes.type "tcp" }}
          startupProbe:
            tcpSocket:
              port: cluster
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
          readinessProbe:
            tcpSocket:
              port: cluster
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
          livenessProbe:
            tcpSocket:
              port: cluster
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
          {{- else if eq .Values.probes.type "http" }}
          startupProbe:
            httpGet:
              path: {{ .Values.probes.http.readyPath }}
              port: metrics
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.http.readyPath }}
              port: metrics
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.http.alivePath }}
              port: metrics
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
          {{- else }}
          {{- fail (printf "probes.type must be 'tcp' or 'http', got %q" .Values.probes.type) }}
          {{- end }}

          lifecycle:
            preStop:
              {{- if eq .Values.preStop.mode "drain" }}
              httpGet:
                path: {{ .Values.preStop.drainPath }}
                port: metrics
              {{- else }}
              exec:
                command:
                  - "/bin/sh"
                  - "-c"
                  - "sleep {{ .Values.preStop.sleepSeconds }}"
              {{- end }}

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          volumeMounts:
            - name: mtls
              mountPath: {{ .Values.mtls.mountPath }}
              readOnly: true
            - name: wal
              mountPath: /var/lib/hyperscale
            - name: tmp
              mountPath: /tmp
            {{- if and (eq .Values.seeds.mode "file") (gt (len .Values.seeds.fileEntries) 0) }}
            - name: seeds
              mountPath: /etc/hyperscale/seeds
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}

      volumes:
        - name: mtls
          secret:
            secretName: {{ .Values.mtls.secret.name }}
            defaultMode: 0400
        - name: wal
          emptyDir:
            {{- if eq .Values.wal.medium "memory" }}
            medium: Memory
            {{- end }}
            sizeLimit: {{ .Values.wal.sizeLimit }}
        - name: tmp
          emptyDir:
            sizeLimit: "64Mi"
        {{- if and (eq .Values.seeds.mode "file") (gt (len .Values.seeds.fileEntries) 0) }}
        - name: seeds
          configMap:
            name: {{ include "hyperscale-manager.seedsConfigMapName" . }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
